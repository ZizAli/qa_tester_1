public class gitCmnds {
}


/*

GIT commands
git --version
cd myproject	cd changes the current working directory.
git commit -help
git help --all
git init	Create new git
git status	Then we check the Git status and see if it is a part of our repo:
git add --all	Using --all instead of individual filenames will stage all changes (new, modified, and deleted) files.
git add .	Add all chnages
git add -A	he shorthand command for git add --all is git add -A
git commit -m "First release of Hello World!"	When we commit, we should always include a message.
git log	To view the history of commits for a repository, you can use the log command
git  branch	We can see in which brnch we are
git beanch <branch name>
git checkout <branch name>	chencge branch
git switch -c <branch name>	creates new branch and chnages to it
git status	to see what was add or changes
git merge <branch name>	merge 2 branchs
git branch -d <branch name>	Delete branch


git remote add origin <link of your branch from github>    git remote add origin URL specifies that you are adding a remote repository, with the specified URL, as an origin to your local Git repo.
git push --set-upstream origin master  Now, go back into GitHub and see that the repository has been updated.


 */